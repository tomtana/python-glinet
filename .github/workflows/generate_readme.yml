# Generate automaticially a markdown README from restructuredtext
name: Generate Markdown Readme

on:
  # Runs on pushes targeting the default branch
  push:
    branches: ["main"]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  # Single deploy job since we're just deploying
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3
      - name: Set up Python 3.x
        uses: actions/setup-python@v4
        with:
          # Semantic version range syntax or exact version of a Python version
          python-version: '3.x'
          # Optional - x64 or x86 architecture, defaults to x64
          architecture: 'x64'
      - name: Convert to Markdown
        run: |
          docker run --rm --volume "$(pwd):/data" --user $(id -u):$(id -g) pandoc/core docs/README.rst --filter docs/conversion_filter.py -o README.md
      - name: Check in README.md
        run: |
          git config --global user.name 'GH Action'
          git config --global user.email 'python@tomtana.net'
          git add README.md
          git commit -m "auto README.md generation"
          git push

